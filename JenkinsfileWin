pipeline {
    agent any
    tools { maven 'M3' }
    stages {
        stage('MAVEN Build') {
            steps {
                bat 'mvn clean package'
            }
        }
        stage('JIB Build IMAGE') {
            steps {
                bat 'mvn jib:build'
            }
        }
    }
}
